# Claude Code instructions

The contributor guide for this repo is in `AGENTS.md`. Claude Code's recursive `@`-import loads it below — keep this file as a 1-line pointer so there is exactly one source of truth.

@AGENTS.md
